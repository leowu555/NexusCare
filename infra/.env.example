# Copy to infra/.env if you want custom local credentials.
# docker compose loads .env from the same folder as the compose file.

POSTGRES_USER=nexuscare
POSTGRES_PASSWORD=nexuscare
POSTGRES_DB=nexuscare

CLICKHOUSE_DB=nexuscare
CLICKHOUSE_USER=nexuscare
CLICKHOUSE_PASSWORD=nexuscare
